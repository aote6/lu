# 炉 (Lu) — 项目状态文档

> 本文档是跨会话的唯一权威状态记录。每次新开对话，先读这份文档，不要要求用户重新解释项目背景。

## 核心结论（最重要，新会话必读）

**当前进度：lu_patch.py 已完成四轮迭代——规则可插拔架构、结构化操作日志、行级/整文件写入模式、写入后语法自动校验+回滚，均已在真实文件上验证通过。**

架构变化：
- 唯一性校验不再写死在 lu_patch.py 主体里，拆成独立规则节点 `rules/001_uniqueness/`（node.json 描述 + check.py 判断逻辑）
- lu_patch.py 主体只负责：扫描 rules/ 目录 → 依次以独立子进程运行每条规则 → 全部通过才原子写入 → 自动快照 → 自动重新锁定
- 规则节点只允许读 target/old/new 三个文件，不给写权限——校验节点没有能力绕过自己去写文件，写入动作永远只发生在 lu_patch.py 主体这一处
- 加新规则 = 在 rules/ 下新增一个文件夹，不需要改 lu_patch.py 本身，这条已验证可行（唯一性校验就是第一个示范）
- 写入换成原子写（tmp 文件 + os.replace + flock 锁目标文件本身），移植自另一个项目内核里已经踩过坑修好的写法，不用裸 open/write
- `--lock`/`--unlock` 成为 lu_patch.py 自己的动词，不是外部 chmod 脚本；写入前自动临时解锁，写完立刻重新锁定

新增能力（本轮完成）：
- **结构化操作日志**：`do_replace`/`do_rollback` 成功失败均记录到 `logs/operations.jsonl`，新增 `rz` 命令查看日志、支持关键词过滤。强制规则清单第4条（结构化执行日志）已落地，不再是待办。
- **行级/范围/整文件写入**：新增 `--line N --text`（改一行一步到位）、`--range S:E --text/--new-file`（按行号替换，不做文本匹配，绕开大段 heredoc 匹配脆弱的问题）、`--whole-file --new-file`（整文件替换，跳过唯一性校验）。
- **写入后语法自动校验+回滚**：`--line`/`--range`/`--whole-file` 写入目标若是 .py 文件，写入后立刻跑 `py_compile` 校验，失败则自动用刚生成的快照回滚，不把损坏状态留在磁盘上。这条是真实事故催生的——测试中曾把 lu_patch.py 自己写出语法错误，导致工具本身跑不动、无法用自己去回滚自己，只能手动 chmod+cp 恢复；加了这层校验后，用构造的语法错误文件验证过：写入失败会自动回滚，退出码为1，原文件内容不变。

真实测试暴露并已修复的问题：
- 之前"改炉自己不用过炉"这个漏洞已经堵上，lu_patch.py 和 STATUS.md 自身的修改都走同一套流程
- 大段 heredoc 一次性粘贴在 Termux 上会丢行、错位——`--range`/`--line` 模式从工具层面直接绕开了这个问题：不再需要构造完整的 old_text 做匹配，按行号定位即可
- **自举死锁的边界**：当 core/lu_patch.py 自身语法损坏到无法被 python3 解析时，没法用 lu_patch.py 去修复自己（连启动都做不到），只能手动 `chmod 644` + `cp` 快照文件 + `chmod 444` 恢复。写入后语法校验大幅降低了这种情况发生的概率，但没有完全消除（比如非 .py 目标文件、或校验本身运行时的意外中断），这条限制本身值得记住，不算 bug

下一步该做什么（按优先级）：

1. 验证期持续观察：`--line`/`--range`/`--whole-file` + 语法校验回滚已投入真实使用，观察是否有边界情况（如非 .py 文件类型的等效校验、多次连续写入的性能）需要补充
2. 两周验证之后，如果确认有效，再考虑加"先读后写"这条规则——做成 rules/ 下新的一个规则节点，不改 lu_patch.py 主体
3. "造语言"依然明确暂不讨论，除非前面规则用了一段时间后发现现有工具/语言层面确实存在绕不开的漏洞

**关于"炉是不是我的落脚点/终极形态"这个问题：** 这个问题讨论已经很充分，答案要靠真实使用的数据说话。期间出现过"炉应该是平台不是工具"这类框架性讨论，其中"规则可插拔、不用改核心文件就能加新能力"这一点已经落地验证，属于真实可用的技术改进；但"炉的终极定位是什么"这个问题本身，仍然不需要继续讨论，新会话如果又滑向这个问题，应该直接引导回"先用起来、记录效果"这条路径上。
## 设计假设（所有强制规则都从这条假设推导，不是拍脑袋加的）

**假设：执行环境里，发出改动指令的可能是一个不具备身体后果意识、可能误解当前文件真实状态的 AI，而不是逐字确认过的人。**

由此推出：任何"写"类操作（改文件、删文件、覆盖内容）都不能默认信任指令本身是准确的，必须由地基本身做兜底校验，而不是依赖"AI这次应该没写错"。

## 强制规则清单（这是核心，也是这个项目真正的棱角所在）

4. **结构化执行日志**（已完成）：每一步操作的输入、校验结果、输出，以结构化 JSON 格式记录在 `logs/operations.jsonl`，`rz` 命令可查看并按关键词过滤。目的是让日志本身能被下一轮 AI 对话直接读取当上下文，不需要再靠人工整理 STATUS.md 摘要（这一点与蛛网的目标可以未来打通，但现阶段不是必须）。

1. **唯一性锚点校验**：任何文本替换类写操作，必须先确认"要被替换的原文"在目标文件中出现且仅出现一次，不满足就物理拒绝写入，而不是提示后仍放行。
   - 沿用并固化你在无界/灵体项目里已经手写验证过的模式：`assert text.count(old) == 1`
2. **先读后写，不允许凭记忆改**：任何改动执行前，地基强制要求最近一次已对目标文件做过完整读取，没有读取记录就拒绝写入。
3. **自动可回滚快照**：每一次写操作自动在时间线上生成快照，不依赖人记得手动 `git commit`。出错时能定位到具体是哪一步、哪一次改动导致的。
4. **结构化执行日志**：每一步操作的输入、校验结果、输出，都以结构化格式（如 JSON）记录，而不是终端里滚动过去就没了的纯文本——目的是让日志本身能被下一轮 AI 对话直接读取当上下文，不需要再靠人工整理 STATUS.md 摘要（这一点与蛛网的目标可以未来打通，但现阶段不是必须）。

## 与 Python／已有工具的关系

- 这一阶段，炉运行环境上执行的语言就是 Python（以及 Java、bash 等现有语言），炉本身不发明新语法。
- 炉的角色是"拦截和记录"，不干涉语言本身的语法或语义。
- 蛛网、灵体、无界现有的 patch 脚本工作流，是炉这套强制规则的雏形和验证依据，不是要被推翻重做，而是要被"从手写脚本约定，升级为运行环境层面强制"。

## 不做什么（防止未来会话滑向功能堆砌）

- 不做新编程语言（等炉的强制规则实际使用验证过，发现"现有语言本身仍允许绕开约束"这个问题真实存在后，再考虑）
- 不做通用 Linux 环境复刻（proot、包管理这类交给 Termux）
- 不做 UI／可视化界面（这一阶段是纯命令行工具）
- 任何新会话如果讨论滑向"要不要加个新功能"，先反问：这条功能是不是在强化"AI 操作者可能出错"这个核心假设下的防护，如果不是，先不做

## 技术选型与实现路径（待细化，先占位）

- 沙盒/权限机制：待定（可能不需要 proot 级别隔离，视炉的定位是否需要隔离执行环境而非仅做写操作拦截）
- 快照存储：初版可考虑基于文件系统的简单快照（如复制到隐藏目录 + 时间戳），不必一开始就依赖 git
- 日志格式：JSON，字段包括操作类型、目标文件、校验结果、时间戳
- 与蛛网的关系：蛛网未来可以读取炉的结构化日志作为"最近改动"的数据源之一，但这是后续整合，不是第一阶段任务

## 开发环境（沿用无界/灵体的既有约定）

- 单人开发者，只用手机（Huawei P20 + Termux），没有 PC、没有 IDE
- 所有代码交互靠复制粘贴终端命令完成
- 打补丁的文件放 `~/lu/.patches/`
- 改代码前先完整读取当前内容，不凭记忆写 patch
- Python 脚本打补丁，固定模式：`assert text.count(old) == 1`

## 项目路径

`~/lu`，与 `~/unbounded`、`~/lingti`、`~/zhiwang` 同级
