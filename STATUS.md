# 炉 (Lu) — 项目状态文档

> 本文档是跨会话的唯一权威状态记录。每次新开对话，先读这份文档，不要要求用户重新解释项目背景。

## 核心结论（最重要，新会话必读）

**当前进度：第一版 `lu_patch.py` 已写完并在真实文件（unbounded/config.py）上完整验证通过。**

已实现并验证的能力：
- 唯一性锚点校验：`--old-file`/`--new-file` 指定原文/新文本，原文在目标文件中必须唯一出现，不满足直接拒绝写入（未找到=退出码1，不唯一=退出码2，并打印各匹配处上下文辅助定位）
- 自动快照：每次成功写入前，自动把原文件备份到 `~/lu/snapshots/<文件名>.<时间戳>.bak`
- 一键回滚：`python3 core/lu_patch.py <文件> --rollback`，自动定位该文件最新快照并恢复，不需要人手动 `ls` 快照目录、手动拼路径

真实测试过程中暴露并已修复的问题：
- 最初回滚要手写 `python3 -c "..."` 拼路径，容易出错且不好用 → 已改为 `--rollback` 子命令，一条命令搞定
- Termux 没有 `/tmp` 目录，测试用的临时文件应放 `~/lu/.patches/` 或 `~/tmp/`，不要假设 `/tmp` 存在
- 代码交付方式：不要用文件下载链接（会强迫用户经过第三方中转到Termux，无法验证中转过程是否失真），**AI 应直接把完整代码贴在聊天消息文本里**，用户直接复制粘贴进 Termux

**下一步该做什么（按优先级）：**

1. **先不要急着加新规则。** 把当前这版 `lu_patch.py` 在无界或灵体的日常真实改动中连续使用一到两周，记录：它到底拦下过几次真的会出错的替换？用起来的额外步骤是否值得？这是决定"炉"这条路值不值得继续投入的关键证据，不是靠讨论"它是不是我的终极形态"能替代的。
2. **两周验证之后，如果确认有效，再考虑加"先读后写"这条规则**——即写入前必须有该文件的最近完整读取记录，没有就拒绝写入。这条规则现在还没做，不要在没验证第一条规则真实有效前提前加。
3. **结构化 JSON 日志（规则4）和"造语言"这两件事，明确暂不讨论**，除非前两条规则用了一段时间后，发现现有工具/语言层面确实存在绕不开的漏洞，那时候的讨论才是从真实需求里长出来的，不是现在凭空决定的。

**关于"炉是不是我的落脚点/终极形态"这个问题：** 这个问题已经反复被问过，讨论已经充分，现阶段不需要继续讨论，答案要靠两周真实使用的数据说话，不是靠继续讨论能得出的。新会话如果又滑向这个问题，应该直接引导回"先用起来、记录效果"这条路径上。
## 设计假设（所有强制规则都从这条假设推导，不是拍脑袋加的）

**假设：执行环境里，发出改动指令的可能是一个不具备身体后果意识、可能误解当前文件真实状态的 AI，而不是逐字确认过的人。**

由此推出：任何"写"类操作（改文件、删文件、覆盖内容）都不能默认信任指令本身是准确的，必须由地基本身做兜底校验，而不是依赖"AI这次应该没写错"。

## 强制规则清单（这是核心，也是这个项目真正的棱角所在）

以下规则要做成**运行环境层面不可绕过**的默认行为，不是可选功能、不是需要记得手动加的参数：

1. **唯一性锚点校验**：任何文本替换类写操作，必须先确认"要被替换的原文"在目标文件中出现且仅出现一次，不满足就物理拒绝写入，而不是提示后仍放行。
   - 沿用并固化你在无界/灵体项目里已经手写验证过的模式：`assert text.count(old) == 1`
2. **先读后写，不允许凭记忆改**：任何改动执行前，地基强制要求最近一次已对目标文件做过完整读取，没有读取记录就拒绝写入。
3. **自动可回滚快照**：每一次写操作自动在时间线上生成快照，不依赖人记得手动 `git commit`。出错时能定位到具体是哪一步、哪一次改动导致的。
4. **结构化执行日志**：每一步操作的输入、校验结果、输出，都以结构化格式（如 JSON）记录，而不是终端里滚动过去就没了的纯文本——目的是让日志本身能被下一轮 AI 对话直接读取当上下文，不需要再靠人工整理 STATUS.md 摘要（这一点与蛛网的目标可以未来打通，但现阶段不是必须）。

## 与 Python／已有工具的关系

- 这一阶段，炉运行环境上执行的语言就是 Python（以及 Java、bash 等现有语言），炉本身不发明新语法。
- 炉的角色是"拦截和记录"，不干涉语言本身的语法或语义。
- 蛛网、灵体、无界现有的 patch 脚本工作流，是炉这套强制规则的雏形和验证依据，不是要被推翻重做，而是要被"从手写脚本约定，升级为运行环境层面强制"。

## 不做什么（防止未来会话滑向功能堆砌）

- 不做新编程语言（等炉的强制规则实际使用验证过，发现"现有语言本身仍允许绕开约束"这个问题真实存在后，再考虑）
- 不做通用 Linux 环境复刻（proot、包管理这类交给 Termux）
- 不做 UI／可视化界面（这一阶段是纯命令行工具）
- 任何新会话如果讨论滑向"要不要加个新功能"，先反问：这条功能是不是在强化"AI 操作者可能出错"这个核心假设下的防护，如果不是，先不做

## 技术选型与实现路径（待细化，先占位）

- 沙盒/权限机制：待定（可能不需要 proot 级别隔离，视炉的定位是否需要隔离执行环境而非仅做写操作拦截）
- 快照存储：初版可考虑基于文件系统的简单快照（如复制到隐藏目录 + 时间戳），不必一开始就依赖 git
- 日志格式：JSON，字段包括操作类型、目标文件、校验结果、时间戳
- 与蛛网的关系：蛛网未来可以读取炉的结构化日志作为"最近改动"的数据源之一，但这是后续整合，不是第一阶段任务

## 开发环境（沿用无界/灵体的既有约定）

- 单人开发者，只用手机（Huawei P20 + Termux），没有 PC、没有 IDE
- 所有代码交互靠复制粘贴终端命令完成
- 打补丁的文件放 `~/lu/.patches/`
- 改代码前先完整读取当前内容，不凭记忆写 patch
- Python 脚本打补丁，固定模式：`assert text.count(old) == 1`

## 项目路径

`~/lu`，与 `~/unbounded`、`~/lingti`、`~/zhiwang` 同级
