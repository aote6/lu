# 炉 (Lu) — 项目状态文档

> 本文档是跨会话的唯一权威状态记录。每次新开对话，先读这份文档，不要要求用户重新解释项目背景。

## 核心结论（最重要，新会话必读）

**当前进度：lu_patch.py 已完成五轮迭代——规则可插拔架构、结构化操作日志、行级/范围/整文件/追加四种写入模式、写入后语法自动校验+回滚、`--text` 高危输入拦截，均已在真实文件（含工具自身、sms、STATUS.md 本身）上多次验证通过，其中多条是被真实事故反向逼出来的，不是预先设计的。**

架构变化：
- 唯一性校验不再写死在 lu_patch.py 主体里，拆成独立规则节点 `rules/001_uniqueness/`（node.json 描述 + check.py 判断逻辑）
- lu_patch.py 主体只负责：扫描 rules/ 目录 → 依次以独立子进程运行每条规则 → 全部通过才原子写入 → 自动快照 → 自动重新锁定
- 规则节点只允许读 target/old/new 三个文件，不给写权限——校验节点没有能力绕过自己去写文件，写入动作永远只发生在 lu_patch.py 主体这一处
- 加新规则 = 在 rules/ 下新增一个文件夹，不需要改 lu_patch.py 本身，这条已验证可行（唯一性校验就是第一个示范）
- 写入换成原子写（tmp 文件 + os.replace + flock 锁目标文件本身），移植自另一个项目内核里已经踩过坑修好的写法，不用裸 open/write
- `--lock`/`--unlock` 成为 lu_patch.py 自己的动词，不是外部 chmod 脚本；写入前自动临时解锁，写完立刻重新锁定

新增能力（本轮完成）：
- **结构化操作日志**：所有写操作（replace/rollback/line_replace/whole_file/append）成功失败均记录到 `logs/operations.jsonl`，`rz` 命令查看日志、支持关键词过滤。强制规则清单第4条已落地。
- **四种写入模式**：`--line N --text`（改一行）、`--range S:E`（按行号范围替换）、`--whole-file`（整文件替换，跳过唯一性校验）、`--append`（追加到文件末尾，不覆盖任何已有内容）。四者共用同一套快照+原子写+日志流程。
- **写入后语法自动校验+回滚**：目标是 .py 文件时，写入后立即 `py_compile` 校验，失败自动回滚到刚生成的快照。这条不是纸面设计——真实操作中曾因手动拼接缩进出错触发 IndentationError，工具当场自动回滚并记录日志，之后重试成功，全过程有 `operations.jsonl` 记录为证。
- **`--text` 高危输入拦截**：`--text` 内容包含换行符，或同时包含单引号和双引号，直接拒绝写入并提示改用 `--new-file`，不再依赖"记得看 STATUS.md"这种约定。这条是真实踩过两次同类坑（嵌套引号被终端打乱、多行内容截断错位）之后，从工具层面直接堵死的，不是文字提醒。

真实事故记录（本轮暴露，已修复或已确认边界）：
- **自举死锁**：core/lu_patch.py 自身语法损坏到无法被 python3 解析时，没法用 lu_patch.py 修复自己，只能手动 `chmod 644` + `cp` 快照 + `chmod 444`。语法自动校验大幅降低了发生概率，但非 .py 文件、或校验运行时本身中断仍是敞口，这条限制记住即可，不强行消除
- **`--line`/`--range` 替换语义的误用**：曾用 `--line` 替换本该"追加"的一行，直接覆盖掉 `.gitignore` 里 `logs/` 这条规则，导致 `logs/operations.jsonl` 被误提交进 git。这不是工具 bug，是"只有 replace 没有 append"这个能力缺口逼出来的误用——已通过新增 `--append` 命令从根上解决，以后"加一行"不再需要借用 replace 硬凑
- **Termux 没有 `/tmp`**：`/tmp/xxx.txt` 这类路径在 Termux 下不存在，会导致 `--new-file` 报 FileNotFoundError。所有临时补丁文件统一放 `~/lu/.patches/`（已在 `.gitignore` 中排除，不会误提交），这条已经是既定约定，本轮又踩了一次，说明约定本身没问题，但容易忘，值得反复提醒新会话
- **临时补丁文件误入库两次**：`status_new_range.txt`、以及一度差点重演的 `patch_*.txt` 系列，均因 `git add -A` 无差别打包而进过暂存区。已在 `.gitignore` 补上 `patch_*.txt` 规则，配合 `.patches/` 目录统一存放临时文件，这条应该已经根治

下一步该做什么（按优先级）：

1. 验证期持续观察：五种写入模式 + 语法校验回滚 + 高危输入拦截已投入真实使用且经历过真实事故考验，继续观察边界情况（如非 .py 文件的等效语法校验）
2. 两周验证之后，如果确认有效，再考虑加"先读后写"这条规则——做成 rules/ 下新的一个规则节点，不改 lu_patch.py 主体
3. "造语言"依然明确暂不讨论，除非前面规则用了一段时间后发现现有工具/语言层面确实存在绕不开的漏洞

**关于"炉是不是我的落脚点/终极形态"这个问题：** 这个问题讨论已经很充分，答案要靠真实使用的数据说话。期间出现过"炉应该是平台不是工具"这类框架性讨论，其中"规则可插拔、不用改核心文件就能加新能力"这一点已经落地验证，属于真实可用的技术改进；但"炉的终极定位是什么"这个问题本身，仍然不需要继续讨论，新会话如果又滑向这个问题，应该直接引导回"先用起来、记录效果"这条路径上。

1. 验证期持续观察：`--line`/`--range`/`--whole-file` + 语法校验回滚已投入真实使用，观察是否有边界情况（如非 .py 文件类型的等效校验、多次连续写入的性能）需要补充
2. 两周验证之后，如果确认有效，再考虑加"先读后写"这条规则——做成 rules/ 下新的一个规则节点，不改 lu_patch.py 主体
3. "造语言"依然明确暂不讨论，除非前面规则用了一段时间后发现现有工具/语言层面确实存在绕不开的漏洞

**关于"炉是不是我的落脚点/终极形态"这个问题：** 这个问题讨论已经很充分，答案要靠真实使用的数据说话。期间出现过"炉应该是平台不是工具"这类框架性讨论，其中"规则可插拔、不用改核心文件就能加新能力"这一点已经落地验证，属于真实可用的技术改进；但"炉的终极定位是什么"这个问题本身，仍然不需要继续讨论，新会话如果又滑向这个问题，应该直接引导回"先用起来、记录效果"这条路径上。
4. **结构化执行日志**（已完成）：每一步操作以结构化 JSON 格式记录在 `logs/operations.jsonl`，`rz` 命令可查看并按关键词过滤。目的是让日志本身能被下一轮 AI 对话直接读取当上下文（这一点与蛛网的目标可以未来打通，但现阶段不是必须）。

**假设：执行环境里，发出改动指令的可能是一个不具备身体后果意识、可能误解当前文件真实状态的 AI，而不是逐字确认过的人。**

由此推出：任何"写"类操作（改文件、删文件、覆盖内容）都不能默认信任指令本身是准确的，必须由地基本身做兜底校验，而不是依赖"AI这次应该没写错"。

## 强制规则清单（这是核心，也是这个项目真正的棱角所在）

4. **结构化执行日志**（已完成）：每一步操作的输入、校验结果、输出，以结构化 JSON 格式记录在 `logs/operations.jsonl`，`rz` 命令可查看并按关键词过滤。目的是让日志本身能被下一轮 AI 对话直接读取当上下文，不需要再靠人工整理 STATUS.md 摘要（这一点与蛛网的目标可以未来打通，但现阶段不是必须）。

1. **唯一性锚点校验**：任何文本替换类写操作，必须先确认"要被替换的原文"在目标文件中出现且仅出现一次，不满足就物理拒绝写入，而不是提示后仍放行。
   - 沿用并固化你在无界/灵体项目里已经手写验证过的模式：`assert text.count(old) == 1`
2. **先读后写，不允许凭记忆改**：任何改动执行前，地基强制要求最近一次已对目标文件做过完整读取，没有读取记录就拒绝写入。
3. **自动可回滚快照**：每一次写操作自动在时间线上生成快照，不依赖人记得手动 `git commit`。出错时能定位到具体是哪一步、哪一次改动导致的。
4. **结构化执行日志**：每一步操作的输入、校验结果、输出，都以结构化格式（如 JSON）记录，而不是终端里滚动过去就没了的纯文本——目的是让日志本身能被下一轮 AI 对话直接读取当上下文，不需要再靠人工整理 STATUS.md 摘要（这一点与蛛网的目标可以未来打通，但现阶段不是必须）。

## 与 Python／已有工具的关系

- 这一阶段，炉运行环境上执行的语言就是 Python（以及 Java、bash 等现有语言），炉本身不发明新语法。
- 炉的角色是"拦截和记录"，不干涉语言本身的语法或语义。
- 蛛网、灵体、无界现有的 patch 脚本工作流，是炉这套强制规则的雏形和验证依据，不是要被推翻重做，而是要被"从手写脚本约定，升级为运行环境层面强制"。

## 不做什么（防止未来会话滑向功能堆砌）

- 不做新编程语言（等炉的强制规则实际使用验证过，发现"现有语言本身仍允许绕开约束"这个问题真实存在后，再考虑）
- 不做通用 Linux 环境复刻（proot、包管理这类交给 Termux）
- 不做 UI／可视化界面（这一阶段是纯命令行工具）
- 任何新会话如果讨论滑向"要不要加个新功能"，先反问：这条功能是不是在强化"AI 操作者可能出错"这个核心假设下的防护，如果不是，先不做

## 技术选型与实现路径（待细化，先占位）

- 沙盒/权限机制：待定（可能不需要 proot 级别隔离，视炉的定位是否需要隔离执行环境而非仅做写操作拦截）
- 快照存储：初版可考虑基于文件系统的简单快照（如复制到隐藏目录 + 时间戳），不必一开始就依赖 git
- 日志格式：JSON，字段包括操作类型、目标文件、校验结果、时间戳
- 与蛛网的关系：蛛网未来可以读取炉的结构化日志作为"最近改动"的数据源之一，但这是后续整合，不是第一阶段任务

## 开发环境（沿用无界/灵体的既有约定）

- 单人开发者，只用手机（Huawei P20 + Termux），没有 PC、没有 IDE
- 所有代码交互靠复制粘贴终端命令完成
- 打补丁的文件放 `~/lu/.patches/`
- 改代码前先完整读取当前内容，不凭记忆写 patch
- Python 脚本打补丁，固定模式：`assert text.count(old) == 1`

## 项目路径

`~/lu`，与 `~/unbounded`、`~/lingti`、`~/zhiwang` 同级


## 2026-07-27 变更：NFKC归一化 + 回滚bug修复

### 新增：全角字符自动修正（NFKC归一化重试）
三处写入操作（line_replace / whole_file / append）的语法校验失败处理中，新增NFKC归一化重试逻辑：
- 触发条件：.py文件 + 语法校验失败 + 错误信息含"invalid character"或"unicode"
- 执行流程：NFKC归一化 -> 重新写入 -> 重新校验 -> 通过则自动修正，失败则回滚到快照
- 日志标记：归一化成功/失败均在operations.jsonl中记录
- 已知代价：.py文件中文注释全角标点会被一同转半角。触发时才生效，非无条件全量转换。此代价已明确接受。
- 测试状态：line_replace / whole_file / append 三条路径均已用真实全角字符case触发验证通过。

### 修复：do_line_replace回滚失效（严重bug）
do_line_replace的except py_compile.PyCompileError分支中，非NFKC路径（普通语法错误）只打印了"自动回滚"消息，但既未实际恢复到快照，也未调用sys.exit退出——控制流继续落到后续的if success判断，导致日志记录"写入成功"而文件实际处于损坏状态。
修复：补全回滚动作（atomic_write恢复快照）+ op_log.log_op记录失败 + sys.exit(1)，与do_whole_file/do_append的异常处理行为一致。已用纯缩进错误case验证通过。

### 验证工具改进
构造patch内容改用Python脚本写文件（python3 << "PYEOF" + with open()），彻底绕过heredoc粘贴。此前在构造normalize_patch.txt时再次发生heredoc截断/错位，被ast.parse预检查拦截。根因一（PTY/heredoc粘贴丢字符）仍未解决，仅靠下游校验兜底。

## 2026-07-27 变更：锚点定位替代手动行号

### 新增：--anchor-line / --anchor-before / --anchor-after

`--line`和`--range`依赖手动传入行号，每次写入后行号偏移，下一次patch前必须重新grep确认——这是结构性的高频摩擦。新增三个锚点参数，用内容定位替代行号定位：

- `--anchor-line "锚点文本"`：替代`--line`，在目标文件中搜索锚点文本（必须唯一），自动计算行号后替换
- `--anchor-before "锚点A"` + `--anchor-after "锚点B"`：替代`--range`，两个锚点各自定位后作为起止行

**实现**：
- 复用`rules/001_uniqueness/check.py`的唯一性判断，不另起一套逻辑
- `check.py`成功态新增一行位置信息（`位置：offset=X line=N`），不改动exit code和原有输出，`run_rules`不受影响
- 锚点定位不进`run_rules`规则链（职责不同：规则链是写入前拦截，锚点是写入前定位），单独调用`check.py`并解析位置行
- 找不到/不唯一时，直接透传`check.py`的原有报错，两处唯一性判断行为完全一致

**测试状态**：
- 单锚点定位：`--anchor-line`与`grep -n`结果一致
- 连续patch不更新行号：第一次patch改变行号后，第二次直接用锚点定位，自动找到正确的新行号
- 锚点不唯一：报错`原文出现 N 次，不唯一`，打印所有匹配位置上下文

### check.py变更

唯一性校验成功时，除原有"唯一性校验：通过"外，新增一行`位置：offset=X line=N`，供锚点定位解析。exit code和原有输出不变。

### 工具自提示

/被调用时自动打印提示，建议改用锚点定位。总纲同步推荐anchor。任何使用者首次接触工具即被告知，不依赖外部解释。

### 锚点空白容错

内新增NFKC+剥离水平空白的容错匹配，容忍终端粘贴引入的全角字符和空格差异（如可正确匹配）。仍保持精确匹配不变。
